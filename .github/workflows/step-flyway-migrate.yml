name: "Step - Flyway Migrate"

on:
  workflow_call:
    inputs:
      env:
        required: true
        type: string

jobs:
  flyway-migrate:
    name: Flyway Migrate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@1.0.0

      - name: Flyway migrate
        run: >
          docker run -v $(pwd)/sql/:/flyway/sql flyway/flyway:9.22-alpine
          -url=jdbc:postgresql://db:5432/a_site_to_order_stuff?sslmode=require
          -user=postgres
          -password=postgres
          -connectRetries=1
          -validateMigrationNaming=true
          migrate
